{
  "asset_id": "funnel",
  "version": 1,
  "source_prompt_file": "application/backend/assets/Prompts/Funnel_Prompt.md",
  "notes": "This prompt's own INPUTS block is unusually thin because it was designed to run inside a continuous chat session and pull almost everything else from conversation history ('pull the full ICP avatar from the ICP output earlier in this conversation', 'pull the rewritten pillar page content from earlier in this conversation', 'pull the competitor analysis from earlier in this conversation'). The context_reference fields below make those implicit reads explicit for the Context Resolver. The synthesized `competitor_analysis` field was removed per an explicit product decision: Funnel has no paired numbered Competitor Analysis file, so the field resolved from `unresolved_context_key` — it could never auto-fill and only ever produced a paste request for a document nothing in the run produces. The prompt's own competitor steps now state their absence instead (see Step 1C / Step 2 in Funnel_Prompt.md), rather than being handed a blank and inventing a competitive landscape.",
  "fields": [
    { "field_id": "funnel_type", "label": "Funnel Type", "kind": "text", "required": true, "source": "user_input", "raw_explanation": "YOUR ANSWER" },
    { "field_id": "lead_magnet_mechanic", "label": "Lead Magnet Mechanic", "kind": "text", "required": true, "source": "user_input", "raw_explanation": "YOUR ANSWER — describe what the visitor receives" },
    { "field_id": "target_service_if_different_from_pillar_page", "label": "Target Service (if different from pillar page)", "kind": "text", "required": false, "source": "user_input", "raw_explanation": "YOUR ANSWER or leave blank" },
    { "field_id": "funnel_entry_points_optional", "label": "Funnel Entry Points (optional)", "kind": "text", "required": false, "source": "user_input", "raw_explanation": "YOUR ANSWER or leave blank" },
    { "field_id": "primary_conversion_goal_optional", "label": "Primary Conversion Goal (optional)", "kind": "text", "required": false, "source": "user_input", "raw_explanation": "YOUR ANSWER or leave blank" },
    { "field_id": "additional_notes_optional", "label": "Additional Notes (optional)", "kind": "text", "required": false, "source": "user_input", "raw_explanation": "YOUR ANSWER or leave blank" },

    { "field_id": "icp_document", "label": "ICP Document", "kind": "context_reference", "required": true, "source": "auto_from_context",
      "context_key": "icp_*", "fallback": "ask_user_if_missing",
      "raw_explanation": "Step 1B: \"Pull the full ICP avatar from the ICP output earlier in this conversation\" — avatar, demographics, psychographics, pains and fears, goals, decision criteria, awareness level, research behaviour, objections, and language. \"This ICP must drive every copy decision in the funnel.\"" },
    { "field_id": "cro_rewritten_copy", "label": "Pillar Page Content (from CRO)", "kind": "context_reference", "required": true, "source": "auto_from_context",
      "context_key": "cro_rewritten_copy", "fallback": "ask_user_if_missing",
      "raw_explanation": "Step 1D: \"Pull the rewritten pillar page content from earlier in this conversation\" — primary/secondary CTA already in use, proof points, differentiators, trust signals, and any copy or claims already established, \"so the funnel reinforces, not contradicts, the pillar page.\"" },
    { "field_id": "cro_locked_sections", "label": "Locked Sections (from CRO)", "kind": "context_reference", "required": true, "source": "auto_from_context",
      "context_key": "cro_locked_sections", "fallback": "ask_user_if_missing",
      "raw_explanation": "Synthesized field — per the cross-cutting DAG rule, any section CRO marked as locked stays locked through every later stage that touches page copy, including Funnel." },
    { "field_id": "design_tokens", "label": "Design Tokens", "kind": "context_reference", "required": true, "source": "auto_from_context",
      "context_key": "design_tokens", "fallback": "ask_user_if_missing",
      "raw_explanation": "Per Marketing_in_a_Box_Session_Context.md §6, Funnel reads icp_*, cro_rewritten_copy, design_tokens — reused so any funnel visuals/pages match the client's established design system." },
    { "field_id": "client_website_url_live_reference", "label": "Client Website URL (live reference)", "kind": "context_reference", "required": false, "source": "live_fetch",
      "context_key": "client_website_url", "fallback": "skip_silently_if_missing",
      "raw_explanation": "Step 1E: \"If any of the above context is incomplete or unclear, browse the client's website (URL established earlier in this conversation) to fill gaps. Do not invent information.\" Per REVIEW_NEEDED_V2.md item 8 resolution, this is a new `source` taxonomy value distinct from user_input/auto_from_context: a live fetch of the client's own site at generation time, not a stored context-store read." }
  ]
}
