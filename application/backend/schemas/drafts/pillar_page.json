{
  "asset_id": "pillar_page",
  "version": 2,
  "source_prompt_file": "application/backend/assets/Prompts/Master_Prompt_Universal_Page_Design_v1.md",
  "paired_main_asset_id": null,
  "notes": "v2 merges the former separate `seo_pillar_page` asset into this single stage. Master_Prompt_Universal_Page_Design_v1.md is now v2.0 of that prompt: it carries the SEO/competitor-benchmark pass (Step 2, Rules 8-9, PART 2 and PART 4) that used to justify a second run of the same file, so there is no longer a design-only pass followed by an SEO variant pass — one stage designs the page from the reference's visual system AND benchmarks its architecture against the competitor pillar pages surfaced by `competitor_analysis_seo_pillar_page` (assets/Prompts/Competitor Analysis/05_SEO_Pillar_Page.md), which is now this stage's paired competitor prepass. `seo_pillar_page.json` was removed in the same change; see DAG_SOURCE_MAP.md row 3.",
  "fields": [
    { "field_id": "client_name", "label": "Client Name", "kind": "text", "required": true, "source": "user_input", "raw_explanation": "YOUR ANSWER" },
    { "field_id": "client_website_url", "label": "Client Website URL", "kind": "text", "required": true, "source": "user_input", "raw_explanation": "YOUR ANSWER" },
    { "field_id": "reference_design_source", "label": "Reference Design Source", "kind": "file_attach", "required": true, "source": "user_input",
      "accepted_types": ["file", "text"], "raw_explanation": "UPLOAD / URL / SCREENSHOT of the page whose visual design to replicate" },
    { "field_id": "reference_design_scope", "label": "Reference Design Scope", "kind": "enum_choice", "required": true, "source": "user_input",
      "choices": ["FULL SITE STYLE", "THIS ONE PAGE ONLY", "A DIFFERENT PAGE ON THE SAME SITE"],
      "raw_explanation": "FULL SITE STYLE / THIS ONE PAGE ONLY / A DIFFERENT PAGE ON THE SAME SITE" },
    { "field_id": "improved_page_content", "label": "Improved Page Content", "kind": "context_reference", "required": true, "source": "auto_from_context",
      "context_key": "cro_rewritten_copy", "fallback": "ask_user_if_missing",
      "raw_explanation": "UPLOAD OR PASTE — the full rewritten page copy. Normally the CRO stage's (Master_Prompt_Universal_Page_Rewrite_v1.md) Part 2 output, but this prompt also runs standalone on any finished copy if pasted directly. If it includes a CRO audit or mode-resolution table, ignore that — design the final page copy only." },
    { "field_id": "cro_locked_sections", "label": "Locked Sections (from CRO)", "kind": "context_reference", "required": true, "source": "auto_from_context",
      "context_key": "cro_locked_sections", "fallback": "ask_user_if_missing",
      "raw_explanation": "Synthesized field — per the cross-cutting DAG rule, any section CRO marked as locked stays locked through every later stage that touches page copy, including Pillar Page." },
    { "field_id": "page_architecture_section_order", "label": "Page Architecture / Section Order", "kind": "text", "required": true, "source": "user_input", "default": "USE DEFAULT",
      "raw_explanation": "LIST ALL SECTIONS IN ORDER, or write \"USE CONTENT ORDER\" to follow the content file's own structure, or \"USE DEFAULT\" for the 13-section universal architecture" },
    { "field_id": "new_sections_to_add_optional", "label": "New Sections to Add (optional)", "kind": "text", "required": false, "source": "user_input", "raw_explanation": "LIST / NONE" },

    { "field_id": "primary_keyword_head_term", "label": "Primary Keyword / Head Term", "kind": "text", "required": true, "source": "user_input",
      "raw_explanation": "YOUR ANSWER — the single term this pillar page must own. Added in v2 with the merged SEO pass: Step 0 item 8 resolves the search-structure requirements from it (single H1 carrying its intent, an H2 per sub-topic, jump navigation, cluster links), and it is also what the paired competitor prepass uses as its {SERVICE} placeholder when searching for competitor pillar pages on this topic." },
    { "field_id": "secondary_cluster_terms_optional", "label": "Secondary / Cluster Terms (optional)", "kind": "text", "required": false, "source": "user_input",
      "raw_explanation": "LIST / NONE" },
    { "field_id": "internal_cluster_pages_to_link_optional", "label": "Internal Cluster Pages to Link (optional)", "kind": "text", "required": false, "source": "user_input",
      "raw_explanation": "LIST URLs + what each covers / NONE. Rule 9 places each of these in the section that page belongs to, with descriptive anchor text — a genuine pillar page links out to its cluster." },
    { "field_id": "competitor_analysis_pillar_page", "label": "Competitor Analysis — Pillar Pages", "kind": "context_reference", "required": false, "source": "auto_from_context",
      "context_key": "competitor_analysis_seo_pillar_page", "fallback": "ask_user_if_missing",
      "raw_explanation": "PASTE OR ATTACH — the competitor pillar-page listing produced by the paired `competitor_analysis_seo_pillar_page` stage (05_SEO_Pillar_Page.md). Not an invisible prepass: because the search has to be run on THIS page's topic (`primary_keyword_head_term`, an intake answer), the UI asks the operator mid-intake whether to research it or to supply their own list, then shows the found competitors for approval before they reach this field. Step 2 extracts architecture, depth and coverage patterns from it and decides ADOPT / EXCEED / SKIP per pattern; competitor copy, claims and branding are explicitly out of bounds. Left unavailable, Step 0 item 7 degrades the run to design-only rather than inventing competitors." },

    { "field_id": "word_for_the_reader", "label": "Word for the reader", "kind": "context_reference", "required": true, "source": "auto_from_context",
      "context_key": "cro_terminology_map", "sub_key": "word_for_the_reader", "fallback": "ask_user_if_missing",
      "raw_explanation": "e.g. patient / client / customer / homeowner / operator. Per REVIEW_NEEDED_V2.md item 4 resolution, auto-pulled from the CRO stage's cro_terminology_map write-key (\"match the copy prompt's resolution — keeps design and content consistent\") instead of being re-entered." },
    { "field_id": "word_for_the_thing_being_chosen_between", "label": "Word for the thing being chosen between", "kind": "context_reference", "required": true, "source": "auto_from_context",
      "context_key": "cro_terminology_map", "sub_key": "word_for_the_thing_being_chosen_between", "fallback": "ask_user_if_missing",
      "raw_explanation": "e.g. treatment / package / tier / plan / model. Auto-pulled from cro_terminology_map." },
    { "field_id": "word_for_the_first_commitment_step", "label": "Word for the first commitment step", "kind": "context_reference", "required": true, "source": "auto_from_context",
      "context_key": "cro_terminology_map", "sub_key": "word_for_the_first_commitment_step", "fallback": "ask_user_if_missing",
      "raw_explanation": "e.g. consultation / site visit / quote / demo / call. Auto-pulled from cro_terminology_map." },

    { "field_id": "primary_cta_label_and_action", "label": "Primary CTA Label + Action", "kind": "text", "required": true, "source": "user_input", "raw_explanation": "e.g. \"Book a free consultation\" → contact form" },
    { "field_id": "secondary_cta_label_and_action_optional", "label": "Secondary CTA Label + Action (optional)", "kind": "text", "required": false, "source": "user_input", "raw_explanation": "YOUR ANSWER / NONE" },

    { "field_id": "image_placement_instructions_optional", "label": "Image Placement Instructions (optional)", "kind": "text", "required": false, "source": "user_input",
      "fallback_instruction": "Follow reference design logic if left blank.", "raw_explanation": "YOUR ANSWER / follow reference design logic if blank" },
    { "field_id": "component_fallback_preference", "label": "Component Fallback Preference", "kind": "enum_choice", "required": true, "source": "user_input",
      "choices": ["STRICT", "ADAPTIVE"], "default": "ADAPTIVE",
      "raw_explanation": "STRICT — skip anything the reference has no equivalent for / ADAPTIVE — build new sections from the closest matching reference pattern (default)" },
    { "field_id": "accessibility_requirement", "label": "Accessibility Requirement", "kind": "enum_choice", "required": true, "source": "user_input",
      "choices": ["STANDARD WCAG AA", "CLIENT-SPECIFIED", "NOT REQUIRED"], "default": "STANDARD WCAG AA",
      "raw_explanation": "STANDARD WCAG AA (default) / CLIENT-SPECIFIED — paste requirements / NOT REQUIRED" },
    { "field_id": "output_format", "label": "Output Format", "kind": "enum_choice", "required": true, "source": "user_input",
      "choices": ["Full HTML + CSS", "HTML sections only", "React component", "Figma-ready component brief", "WordPress block structure"],
      "raw_explanation": "Full HTML + CSS / HTML sections only / React component / Figma-ready component brief / WordPress block structure" },
    { "field_id": "additional_notes_constraints_optional", "label": "Additional Notes / Constraints (optional)", "kind": "text", "required": false, "source": "user_input", "raw_explanation": "YOUR ANSWER / NONE" }
  ]
}
